{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
    {% for post in post %}
        <article>
            <h2>{{ post.title }}</h2>
            <p>{{ post.description }}</p>
        </article>
    {% endfor %}
{% endblock %}